// Single source of truth for the product's user-facing name. Replaces Postiz's
// hardcoded `isGeneralServerSide() ? 'Postiz' : 'Gitroom'` brand branches so the
// rebrand lives in one place (and future upstream merges only touch here).
export const BRAND_NAME = 'DuabaConnect';
